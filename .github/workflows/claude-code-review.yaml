name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]

jobs:
  claude-code-review:
    uses: devrev/.github/.github/workflows/claude-code-review.yml@main
    secrets: inherit
